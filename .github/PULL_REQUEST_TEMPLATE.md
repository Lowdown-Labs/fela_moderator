<!-- Thanks for contributing! Keep it lean, deterministic, offline. -->

## What & why

<!-- What does this change and why? Link any issue. -->

## Checklist

- [ ] `npm test` passes (node suites + vitest)
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] `npm run typecheck` passes
- [ ] Added/updated tests for the change
- [ ] Added a changeset (`npm run changeset`) if user-facing
- [ ] No new network I/O on the user-data path; any new dependency is MIT/permissive and recorded in `LICENSES.md`
